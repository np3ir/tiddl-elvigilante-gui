flet==0.86.1
tomlkit>=0.13
# tiddl empaquetado DENTRO del app (binario unico): flet build lee este
# requirements e incluye el paquete, asi que la GUI corre tiddl in-process y
# no necesita un tiddl.exe aparte ni que el usuario lo instale. Apunta al fork
# corregido en main (incluye el fix del selector de calidad y, tras mergear
# feat/cancel-hook, el hook de cancelacion cooperativa para el modo in-process).
tiddl-elvigilante @ git+https://github.com/np3ir/tiddl-elvigilante.git@main
