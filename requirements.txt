flet==0.86.1
tomlkit>=0.13
# tiddl is bundled INSIDE the app (single binary): flet build reads this
# requirements file and includes the package, so the GUI runs tiddl in-process
# with no separate tiddl.exe. Pinned to the reviewed engine commit containing
# stereo-edition resolution, flexible/strict quality policy, playback-manifest
# validation, run-wide 401 handling, and the existing NAS/download hardening.
# This revision (engine v1.3.1 + dedup polish) also adds stereo resolution for
# ARTIST URLs (expands an artist into its albums resolved to stereo, keeping an
# album's original when it has no stereo edition), the non-zero exit on a safety
# stop, and the strict-Normal -> TIDAL HIGH mapping fix.
tiddl-elvigilante @ git+https://github.com/np3ir/tiddl-elvigilante.git@91b7d629f23b2d549213e2fe57659a1c8f516575
